grumphp:
    tasks:
        phpstan:
            config: phpstan.neon.dist
            memory_limit: 512M
        phpcsfixer:
            config: .php-cs-fixer.dist.php
        phpunit:
            config: phpunit.xml.dist
        deptrac: ~
        infection:
            only_covered: true
            min_covered_msi: 65
            show_mutations: true
            threads: max
